import React from 'react';

const FeaturePage = ({ title, img, info }) => (
  <div className="featurepage">
    TODO FeaturePage
    <h1>{title}</h1>
    <img src={img} alt={title} />
  </div>
);

export default FeaturePage;